# JWT Configuration (OBLIGATORIO para producción)
# Mínimo 32 caracteres. Se usa para firmar los tokens JWT.
JWT_SECRET_KEY=your-secret-key-here-minimum-32-characters-long

# Encryption Key (OBLIGATORIO para producción)
# Debe tener exactamente 32 caracteres. Se usa para encriptar la API Key de OpenAI en la BD.
ENCRYPTION_KEY=your-encryption-key-exactly-32-characters-long

# PostgreSQL Database Configuration (para Docker Compose)
POSTGRES_DB=housespend
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-postgres-password-here

# Database Connection (for local development without Docker)
# DATABASE_URL=Host=localhost;Port=5432;Database=housespend;Username=postgres;Password=your-password

# Frontend API URL
NEXT_PUBLIC_API_URL=http://localhost:5000/api

